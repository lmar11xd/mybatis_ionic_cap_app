import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lmar.mybatisapp',
  appName: 'ionic_mybatis_app',
  webDir: 'dist/ionic_mybatis_app',
  bundledWebRuntime: false
};

export default config;
